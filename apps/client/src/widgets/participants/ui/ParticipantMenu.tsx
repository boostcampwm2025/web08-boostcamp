import { MenuButton } from '@codejam/ui';

/**
 * 참가자 옵션 메뉴 버튼
 * - 추후 권한 관리, 강퇴 등의 기능 추가 예정
 */
export function ParticipantMenu() {
  // TODO: 메뉴 동작 구현
  const handleMenuClick = () => {
    // 메뉴 열기 로직
  };

  return <MenuButton onClick={handleMenuClick} />;
}
